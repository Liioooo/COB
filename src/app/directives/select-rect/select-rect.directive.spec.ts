import {SelectRectDirective} from './select-rect.directive';

describe('SelectRectDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectRectDirective();
    expect(directive).toBeTruthy();
  });
});
